
# 后端联调待确认清单 (Frontend to Backend)

| 编号 | 模块 | 疑问/差异点 | 建议方案 | 状态 |
| :--- | :--- | :--- | :--- | :--- |
| **Q1** | 封包适配 | 契约 B 的 `code` 成功值是 0 还是 200？ | 目前代码兼容 0 和 200。 | 待确认 |
| **Q2** | 错误码映射 | `E001` 余额不足时，后端是否能返回 `rawCode: 20001`？ | 映射表已按 20001 准备。 | 待确认 |
| **Q3** | 充值流水 | `POST /wallet/recharge` 是否需要返回充值后的即时余额？ | 建议返回 `balanceAfter` 以同步前端显示。 | 待建议 |
| **Q4** | 扫码支付 | H5 扫码后是直接调用 `orders/checkout` 还是调起微信支付 SDK？ | 目前按 API 扣款流程实现。 | 待明确 |
| **Q5** | 幂等 ID | `X-Request-Id` 的有效期建议设置为多少分钟？ | 建议 5-10 分钟。 | 待讨论 |
